
Interrupts:
    buttons : |
        | name    | type      | scenarios |

HTML:
    - name: home.test1.but_test
      type: button
      background_image: cuisine_1.png
      inactive_color: black
      active_color: grey
      action:
          type: button
          button: principal
          scenarios: allumer, eteindre

Scenarios:
    - name: eteindre
      marker: OFF
      instructions: |
          | name | type | delay | duration | args |

    - name: allumer
      marker: ON
      instructions: |
          | name                                   | type             | delay | duration | args |
